# How to update query

## Open Datasette Lite with `puzzles_enriched_puzzle_storm`

## Create new filter or aggregation query

## Check if any fields should be added to enrich

Fields like `result_int` are applicable at the row level, so they apply to every puzzle.

## Update `src/pipeline.sh`