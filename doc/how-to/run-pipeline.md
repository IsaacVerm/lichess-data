# How to run the data pipeline

Make sure [sqlite-utils](https://sqlite-utils.datasette.io/en/stable/) is installed.

```
./src/pipeline.sh
```

The script automatically removes previous data to make sure we always have the latest version.